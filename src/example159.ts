// src/example159.ts


// Combine the numbers of a given array into an array containing all combinations.
export function example159(inputArray: number[]): number[] {
    return Array.from(inputArray)
}

