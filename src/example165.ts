// src/example165.ts


// Return the minimum-maximum value of an array, after applying the provided function to set a comparing.
export function example165(inputString: string): number {
    return new Blob([inputString], {type: 'text/plain'}).size
}

