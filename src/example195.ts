// src/example195.ts


// Split values into two groups. If an element in the filter is true, 
// the corresponding element in the collection belongs to the first group;
// otherwise, it belongs to the second group.
export function example195(inputArray: any[], filter: (x: any) => boolean): [any[], any[]] {
    return [[], []] // inputArray.filter((x) => x, inputArray)
}
