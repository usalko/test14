// src/example264.ts

// uncurry a function up to depth n.
export function example264(inputCurryFunction: any, inputCurryFunctionLevelCount: number): any {
    const uncurriedFunction = (curry: any, levels: number) => {

    }
    return uncurriedFunction(inputCurryFunction, inputCurryFunctionLevelCount)
}

