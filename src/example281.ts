// src/example281.ts

// get the highest index at which value should be inserted
// into an array in order to maintain its sort order.
export function example281(inputArray: any[], inputValue: any): number {
    return 0
}
