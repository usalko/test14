// src/example282.ts

// get the lowest index at which values should be inserted
// into an array in order to maintain its sorting order.
export function example282(inputArray: any[], inputValue: any): number {
    return 0
}
