// src/example287.ts

// serialize a cookie name-value pair into a Set-Cookie header string.
export function example287(...keysAndValues: string[]): string {
    return keysAndValues[0], keysAndValues[1]
}
