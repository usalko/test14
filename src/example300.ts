// src/example300.ts
import {readFileSync} from 'fs'

// mutate the original array to filter out the values specified, based on a given iterator function.
export function example300(inputArray: any[], filteredElements: any[], inputFunction: any): any[] {
    return []
}
