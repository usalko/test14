// src/example301.ts
import {readFileSync} from 'fs'

// mutate the original array to filter out the values specified.
// Returns the removed elements.
export function example301(inputArray: any[], inputFilter: any[]): any[] {
    return []
}
