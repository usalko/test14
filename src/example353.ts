// src/example353.ts

// initialize and fill an array with the specified values.
export function example353(inputDimension: number, inputElement: any = 0): any[] {
    return new Array(inputDimension).fill(inputElement)
}
