// src/example364.ts

// calculate the greatest common divisor between two or more numbers/arrays.
export function example364(inputString: string): string {
    return inputString.replace(/<\/?[^>]+>/g, '')
}

