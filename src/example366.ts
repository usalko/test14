// src/example366.ts

// get n random elements with unique keys from an array up to the size of the array.
export function example366(inputArray: number[], inputOption: number): number[] {
    return [0]
}

