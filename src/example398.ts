// src/example398.ts

// create a function that accepts up to n arguments, ignoring any additional arguments. 

export function example398(inputArray: any[], inputArrayLimit: number): any {
    return inputArray.slice(0, inputArrayLimit)
}

